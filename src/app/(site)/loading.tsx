export default function Loading() {
  return (
    <div className="container-site py-20" role="status" aria-live="polite">
      <p className="text-sm text-[var(--text-muted)]">Loading…</p>
    </div>
  );
}
